{
  "name": "openflow",
  "version": "0.1.0",
  "description": "Lightweight personal AI assistant with Telegram integration",
  "type": "module",
  "main": "dist/index.js",
  "bin": {
    "openflow": "dist/bin.js"
  },
  "scripts": {
    "dev": "tsx src/bin.ts",
    "build": "tsdown",
    "typecheck": "tsc --noEmit",
    "lint": "oxlint src/",
    "format": "oxfmt --write src/",
    "format:check": "oxfmt --check src/",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "check": "oxlint src/ && oxfmt --check src/ && tsc --noEmit && vitest run",
    "start": "node dist/bin.js"
  },
  "files": [
    "dist/"
  ],
  "dependencies": {
    "@clack/prompts": "^1.2.0",
    "@grammyjs/runner": "^2.0.3",
    "@grammyjs/transformer-throttler": "^1.2.1",
    "better-sqlite3": "^11.9.1",
    "grammy": "^1.36.3",
    "pino": "^9.6.0",
    "undici": "^8.1.0",
    "zod": "^3.24.4"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^7.6.13",
    "@types/node": "^22.15.2",
    "oxlint": "^0.16.10",
    "tsdown": "^0.12.3",
    "tsx": "^4.19.3",
    "typescript": "^5.8.3",
    "vitest": "^3.1.2"
  },
  "engines": {
    "node": ">=22.12.0"
  },
  "license": "MIT",
  "onlyBuiltDependencies": [
    "better-sqlite3",
    "esbuild"
  ]
}
